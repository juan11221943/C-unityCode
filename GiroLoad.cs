using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GiroLoad : MonoBehaviour
{
    

    // Update is called once per frame
    void Update()
    {
       this.transform.Rotate(1f,0,0); 
    }
}
